import 'package:flutter/material.dart';

class Pipenote extends StatelessWidget {
  const Pipenote({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp();
  }
}
